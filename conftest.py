"""
Pytest configuration (e.g. for cache dir or shared fixtures).
Cache is disabled via -p no:cacheprovider in pytest.ini to avoid permission
warnings under Dropbox or locked project dirs.
"""
# Test config supports fixtures for Pulse private memory, Intel tab, and Shield security tests (test pillar support)
# Pulse private memory + Shield (conftest surface)

# Pulse private memory + Shield (conftest additional surface for pillar awareness)
